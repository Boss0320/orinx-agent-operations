# Publication review record

This record separates local engineering evidence from publication authority. A green local row does not
authorize a remote repository, public visibility, or a claim about current operation.

| Gate | Verification command | Observed output | Root closed by | Residual risk |
|---|---|---|---|---|
| Claim-to-test mapping | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 46 passed, 0 failures, 0 errors | Behavioral suites, input contracts, scanner, deterministic examples, evidence-window, and paired-lifecycle regressions | Clean-room tests do not prove external integration behavior |
| Original-source isolation | `python3 scripts/check_public_boundary.py .` | `[]`, exit 0 in working tree | Default-deny private-path and identifier rules | Git history does not exist yet; inspect after initialization |
| Synthetic-data boundary | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_public_boundary tests.test_input_contracts -v` | 19 passed | Scanner rejects sensitive shapes, environment files, runtime caches, unknown file types, and non-synthetic literals; models reject non-synthetic runtime inputs | Reviewer must repeat after Git initialization |
| Kill switch and ownership | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_reconciliation -v` | 13 passed | Action validator plus ownership, direction／lifecycle／quantity／status mismatch, ambiguity, kill-switch, and bounded evidence-window tests | Clean-room demo does not prove external integration behavior |
| Watchdog common-cause boundary | `python3 -m unittest tests.test_watchdog_authority -v` | 10 passed | Missing heartbeat stays unhealthy; cooldown cannot suppress recovery; restart authority is single-use | Real independent-heartbeat deployment is not part of this repository |
| Prohibited public content | `python3 scripts/check_public_boundary.py .` | `[]`, exit 0 in working tree | Stable scanner with redacted findings | Independent adversarial content review pending |
| Markdown links and diagram render | Local link check, XML validation, and 390px／1440px render QA | Fresh copy: 76 links checked, 0 broken; 3 SVGs XML-clean; final 16px-body mobile renders visually inspected without clipping | Mobile-first 390px viewBox, explicit line wrapping, and accessible SVG metadata | GitHub Markdown rendering still needs post-push review |
| Adversarial self-review | Focused RED tests for semantic mismatch, paired lifecycle identity, evidence freshness, cooldown／restart authority, symlinks, runtime caches, dotfiles, private keys, and current-live copy | New regressions failed before their fixes and passed afterward; final suite 46 passed | Finding types, evidence identity／validity, watchdog order, input contracts, and scanner rules | Clean-room scope cannot prove live execution behavior |
| Independent adversarial review | Reviewer attacks authority, attribution, implied live state, evidence identity／freshness, documentation order, scanner fail-closed behavior, and diagram readability | Final fixes-only follow-up: Critical 0, Important 0, Minor 0; local publication review gate PASS | Reviewer independently reproduced 46／46 tests, lifecycle fail-closed behavior, boundary `[]`, runtime-cache rejection, and XML-clean SVGs | GitHub rendering, remote creation, and Boss publication approval remain separate gates |
| License | Boss decision recorded 2026-08-27 | MIT selected — publication gate resolved | Standard MIT license in `LICENSE` | Permissive reuse of the clean-room demo is intentional |
| Repository slug | Boss decision recorded 2026-08-27 | `orinx-agent-operations` selected — naming gate resolved | Exact public repository name is frozen before remote creation | Selecting a slug does not create or publish a remote |
| Remote, push, public toggle | Explicit Boss publication approval | Not approved — blocks publication | Pending | No remote exists |
| Packaging-upgrade RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_packaging_contract -v` | `Ran 4 tests`; `FAILED (failures=4)`: old H1 retained the bounded-autonomy descriptor, the incident reconstruction was absent, clean-room copy preceded the value proposition, and the old SVG used `0 0 390 1000` without the required split-state labels. | New contracts were executed before README, article, or SVG production edits. | RED proves the approved packaging structure was absent; it does not assess final writing or rendered geometry. |
| Packaging hierarchy and incident contracts | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_packaging_contract -v` | `Ran 4 tests in 0.001s`; `OK`. | Tests require product-only H1s, separate bilingual descriptor／thesis／ownership layers, ordered before→failure→decision→permanent-fix arcs, clean-room copy after the incident, a `390 × 1040` split-authority board, exact state/recovery markers, and a `16px` static text floor. | Source contracts cannot prove final GitHub Markdown rendering or browser font metrics. |
| Packaging full local gates | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v`; two runs of both deterministic examples; `python3 scripts/check_public_boundary.py .`; three-SVG XML parse; recursive Markdown link check | `Ran 50 tests in 0.009s`; `OK`; example runs exited `0`, were byte-identical, and produced `1149` bytes; boundary `[]`; `SVG XML: 3 parsed`; `Checked 80 Markdown links: 0 broken.` | Every prior reconciliation, watchdog, input, scanner, and example contract passes with the new content layer; the scanner still rejects live, performance, secret, runtime-cache, and prohibited-autonomy shapes. | Clean-room regression tests do not prove exchange integration, performance, or current operation. |
| Packaging manifest-equivalent fresh copy | Copy the source tree into the empty `/private/tmp/orinx-agent-operations-packaging-accepted.2ZyHFe`; compare sorted inventory and every relative file byte; repeat full tests, examples, boundary, XML, links, file count, and symlink scan in the copy | Source and fresh copy contained the same `37` regular files with no byte difference and zero symlinks; fresh `Ran 50 tests in 0.009s`, `OK`; examples were byte-identical at `1149` bytes; boundary `[]`; three SVGs parsed; `80` links, `0` broken. | The final package behavior reproduces from a new system-temp directory without relying on cache, runtime, network, or undeclared source bytes. | This tree has no allowlist assembler; exact inventory／byte comparison is local evidence, not a Git or publication grant. |
| Split-authority real-browser QA | Serve the fresh copy on `127.0.0.1`; open `assets/failure-timeline.svg` directly at `1440 × 1000` and `390 × 844`; inspect screenshot, computed text, focal-state geometry, and horizontal overflow | Both viewports rendered the full `390 × 1040` board with `29` text nodes, computed text range `16–22px`, and no horizontal overflow. At mobile width the SVG occupied exactly `390px`; all LOCAL CLOSED／EXCHANGE OPEN focal nodes remained inside the viewport. The split state, preserve-both decision, bounded retry, re-observation, and human boundary were visually legible. | The paired state board makes the incident consequence visible before the recovery matrix and closes the previous generic-timeline weakness. | The in-app surface emitted one injected `animation` promise error while viewing the script-free raw SVG; XML, source scan, and the asset itself contain no script. GitHub Markdown rendering remains a post-push gate. |
| Packaging adversarial self-attack | Compare the changed bilingual openings, incident stages, and SVG labels against the current claims／must-drop contract; focused `rg -n -i` scan for prohibited positioning, current-operation, performance, and bilingual ownership／incident drift | Critical `0`. Current operation and performance terms appear only in explicit exclusions; English uses past-tense operating claims and Chinese uses `曾`／`在長期運作期間` semantics; both versions preserve one-retry, re-observation, ownership, kill-switch, and human-escalation limits. Warning: final GitHub Markdown appearance is not locally reproducible. | Claims remain assertive while staying historical, bounded, and attributable; the scanner and content tests make the main drift classes permanent regressions. | Public perception still requires a post-push visual review and human factual challenge; no remote or publication is approved. |
| Private remote and initial push (2026-08-29) | Boss-authorized execution: per-repository Git identity `Titus Lai`; MIT `LICENSE`; a single initial commit pushed to the private GitHub remote `orinx-agent-operations`; full-history `gitleaks` 8.30.1 content scan; post-push GitHub Markdown QA of both READMEs and all three SVG diagrams | The private remote holds the single-commit history; gitleaks reported no leaks; GitHub rendering QA passed | Supersedes the earlier "Remote, push, public toggle — Not approved / No remote exists" row for remote creation and the private push, and closes the post-push GitHub Markdown rendering gate | The repository stays private; the public toggle and an independent external review before publication remain open Boss gates |
