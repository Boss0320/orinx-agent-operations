[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "orinx-agent-operations"
version = "0.1.0"
description = "Offline clean-room demos for bounded agent operations"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["demo*"]

